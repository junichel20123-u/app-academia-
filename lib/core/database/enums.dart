enum MuscleGroup { chest, back, legs, shoulders, arms, core, fullBody, cardio }

enum Equipment {
  barbell,
  dumbbell,
  machine,
  bodyweight,
  cable,
  kettlebell,
  band,
  other,
}

enum ExerciseVideoStatus { idle, generating, ready, failed }

enum WorkoutSessionStatus { inProgress, completed, abandoned }

enum CardioActivityType { run, bike, walk, swim, other }

enum UnitSystem { metric, imperial }

enum AppThemeMode { dark, light, system }
